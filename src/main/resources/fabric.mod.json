{
  "schemaVersion": 1,
  "id": "pvpclient",
  "version": "${version}",
  "name": "PvP Client",
  "description": "Advanced PvP Client with modular features",
  "authors": ["PvPClient Dev"],
  "contact": {},
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "main": [
      "com.pvpclient.PvPClient"
    ]
  },
  "mixins": [
    "pvpclient.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": ">=${fabric_version}",
    "minecraft": "${minecraft_version}"
  }
}
